import { Meta, Canvas, ArgsTable, Story } from "@storybook/blocks";
import BotomPopup from "./";

<Meta title="Primitive/BotomPopup" component={BotomPopup} />

# BotomPopup

프로젝트 내에서 사용되는 BotomPopup 컴포넌트 입니다. (주로, ClickSelectBox의 children Wrapper Component로 사용됩니다.)

## BottomPopupProps

```ts
interface BottomPopupProps {
  /**
   * BottomPopup안의 자식 컴포넌트.
   */
  children: React.ReactNode;
}
```

import { Meta, Canvas, ArgsTable, Story } from "@storybook/blocks";
import Header from "./";

<Meta title="Primitive/Header" component={Header} />

# Header

프로젝트 내에서 로그인 이후에 보여지는 헤더입니다. (왼쪽엔 로고가, 오른쪽엔 부가 기능을 이용할 수 있는 컴포넌트가 담겨있습니다.)

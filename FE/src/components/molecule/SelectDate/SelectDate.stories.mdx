import { Meta, Canvas, ArgsTable, Story } from "@storybook/blocks";
import SelectDate from "./";

<Meta title="Primitive/SelectDate" component={SelectDate} />

# SelectDate

"월"에 따라 데이터가 변경되어지는 컴포넌트로, 왼쪽 / 오른쪽 버튼 컴포넌트 각각 클릭시 월이 변경되는 컴포넌트
